
function calculateTip() {
  var cartSubtotal = document.getElementById("cart").innerHTML;
  var tip = document.getElementById("tip");
  var totalAndTip = cartSubtotal + tip;
  console.log(cartSubtotal)
}